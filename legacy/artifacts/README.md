This directory contains historical incident artifacts and ad hoc logs kept for forensics/reference.

They are intentionally quarantined from runtime source (`src/`) and CI/runtime Postgres migration guards.

These files may include legacy SQLite-era stack traces or operational notes and should not be treated as current implementation guidance.
