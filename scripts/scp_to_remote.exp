#!/usr/bin/expect
set timeout 300
set local_file [lindex $argv 0]
set remote_dest [lindex $argv 1]
set password [lindex $argv 2]

spawn scp -o StrictHostKeyChecking=no $local_file $remote_dest
expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
